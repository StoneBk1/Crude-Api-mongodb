<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <link rel="stylesheet" href="styles/products.css">
</head>
<body>
    <div class="container">
        <h1>All Products</h1>
        <ul class="product-list">
            <% allProducts.forEach(product => { %>
                <li class="product-item"><a href="/single-product/<%= product._id %>"><%= product.name %></a></li>
           <% }); %>
        </ul>
        <a href="/create" class="create-link">Create a new Product</a>
    </div>
</body>
</html>
